// const labels = document.querySelectorAll('.questions li label');
//     labels.forEach(label => {
//         label.addEventListener('click', function () {
//             const isChecked = this.previousElementSibling.checked;
//             if (isChecked) {
//                 // If radio is already checked, uncheck it
//                 this.previousElementSibling.checked = false;
//             }
//         });
//     // });


    // Get all question elements
   // const questions = document.querySelectorAll('.faq .questions li');

    // Loop through each question element
    // questions.forEach(question => {
    //     // Add click event listener to each question
    //     question.addEventListener('click', () => {
    //         // Toggle the 'active' class on the question
    //         question.classList.toggle('active');

    //         // Toggle the visibility of the answer
    //         const answer = question.querySelector('.ans');
    //         if (answer.style.maxHeight) {
    //             // If answer is currently visible, hide it
    //             answer.style.maxHeight = null;
    //         } else {
    //             // If answer is currently hidden, show it
    //             answer.style.maxHeight = answer.scrollHeight + 'px';
    //         }
    //     });
    // });

